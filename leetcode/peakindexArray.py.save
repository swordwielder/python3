class Solution:
    def peakIndexInMountainArray(self, A: List[int]) -> int:
        
        peak=0
        indexcount=0
        for index in range(len(A)):
            if A[index] > peak:
                peak = A[index]
                indexcount = index
        
            return indexcount
